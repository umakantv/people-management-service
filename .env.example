# Database configuration
DRIVER=sqlite3
DB=people.db

# Optional fields for MySQL/PostgreSQL:
# HOST=localhost
# PORT=3306
# USER=root
# PASSWORD=

# FTS5 Full-Text Search (requires `go run --tags "fts5" main.go`)
# USE_FTS=true  # Enable FTS5-powered search; falls back to LIKE if unavailable
